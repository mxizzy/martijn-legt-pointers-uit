// create a linked list, acts as a list, which is like an expandable array, this exists out of structs pointing to each other in only one direction
// struct (met data erin) -> struct (met data erin) -> struct (met data erin)
// need pointers, structs, pointing at objects in structs
#include <stdio.h>

